<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="text">
      <p>{{comment_text}}</p>
      <p>{{user.username}}, {{format_date created_at}}</p>
    </div>
  </section>
  {{/each}}
</div>
